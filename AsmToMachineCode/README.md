This project is a small assembler written in python language.
It gets an instruction in assembly language and prints the corresponding
machine code. It is written to do operators like 'mov', 'sub', 'xor', 'mul',
and 'not'.
